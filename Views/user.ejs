<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Users</title>
  </head>
  <body>
      <div>
        <h2>Users</h2>
        <table>
          <% for(var i=0; i < dataUser.users.length; i++) { %>
   <tr>
     <td><%= dataUser.users[i].username %></td>
     <td><%= dataUser.users[i].email %></td>
   </tr>
<% } %>
      </table>
      </div>
  </body>
</html>
