<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>List of USERS</title>
</head>
<body>
	<h1>List of Users</h1>

	<% for (var i = 0; i < users.length; i++ ) { %>
		<p>ID: <%= users[i].id %> Username: <%= users[i].username %> Password: <%= users[i].password %> Email: <%= users[i].email %> <a href="/users/edit/<%= users[i].id %>">Edit</a>  <a href="/users/delete/<%= users[i].id %>">Delete</a></p>
	<% } %>
</body>
</html>