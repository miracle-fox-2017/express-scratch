<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Users List</title>
  </head>
  <body>
    <h1>USERS:</h1>
    <a href="/">go back home</a> || <a href="/cities/">list cities</a> || <a href="/users/">list users</a><br><br>
    <% for(let i = 0; i<users.length; i++){ %>
      <%= users[i].id%>. 
      <%= users[i].username%>
      <%= users[i].password%>
      <%= users[i].email%> <a href="/users/edit/<%= users[i].id%>">edit</a> <a href="/users/delete/<%= users[i].id%>">delete</a><br>
      
    <% } %>
    <br>
    <a href="/users/add">add user</a><br>
  </body>
</html>
